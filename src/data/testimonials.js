/**
 * Depoimentos reais de clientes.
 *
 * Propositalmente vazio: nenhum depoimento fictício é exibido no site.
 * Assim que houver avaliações reais, adicione objetos neste array no
 * formato abaixo e o carrossel em components/Testimonials passa a
 * exibi-los automaticamente — nenhuma alteração de código é necessária.
 *
 * { id: string, name: string, role: string, quote: string, rating: 1-5 }
 */
export const testimonials = []
